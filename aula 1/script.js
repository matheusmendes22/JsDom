// Retorne o url da página atual utilizando o objeto window
let urlPagina = window.location.href;
console.log(urlPagina)
// Seleciona o primeiro elemento da página que
// possua a classe ativo
let itemAtivo = document.querySelector('.ativo')
console.log(itemAtivo)
// Retorne a linguagem do navegador
let navegador = window.navigator;
console.log(navegador.appCodeName)
// Retorne a largura da janela 
let larguraPagina = innerWidth;
console.log(larguraPagina)